services:
  db:
    image: postgres:15
    container_name: db_book
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: db_book
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    volumes:
      - db-book-data:/var/lib/postgresql/data
volumes:
  db-book-data: